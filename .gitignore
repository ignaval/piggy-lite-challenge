# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Belt-and-suspenders (each app also has its own .gitignore)
**/.venv/
**/node_modules/
**/.next/
*.db
.env
**/.env.local

# Internal: keep reviewer notes out of the candidate-facing repo (see the file's
# header). Delete this line if you maintain a separate internal source repo.
/NOTES_FOR_REVIEWERS.md
