# Pin-free, pip-installable mirror of pyproject.toml runtime + dev deps.
# Prefer `uv sync` (see README); this exists for plain `pip install -r`.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlmodel>=0.0.16
pydantic-settings>=2.1
alembic>=1.13
httpx>=0.27

# dev / test
pytest>=8.0
ruff>=0.4
